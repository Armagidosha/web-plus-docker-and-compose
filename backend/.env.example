# Environment variables configuration for the backend application
#
# DB_TYPE: database type (postgres, mysql, etc.)
# POSTGRES_HOST: host address of the PostgreSQL server
# POSTGRES_PORT: port number of the PostgreSQL server
# POSTGRES_DB: name of the database
# POSTGRES_USER: username for the database
# POSTGRES_PASSWORD: password for the database
# JWTSECRET: secret key for JSON Web Token (JWT)
# TTL: time to live for JWT (in days)
# MODE: application mode (dev, prod, test, etc.)

# Database configuration
DB_TYPE='postgres'                 # Database type
POSTGRES_HOST=localhost          # Host address of the PostgreSQL server
POSTGRES_PORT=5432               # Port number of the PostgreSQL server
POSTGRES_DB=kupipodariday          # Name of the database
POSTGRES_USER=admin              # Username for the database
POSTGRES_PASSWORD=admin          # Password for the database

# JWT configuration
JWTSECRET=TheMostSecretKey        # Secret key for JSON Web Token (JWT)
TTL=30                           # Time to live for JWT (in days)

# Application mode
MODE=dev                          # Application mode (dev, prod, test, etc.)

# PostgreSQL configuration
PGDATA=your_path                  # Path where PostgreSQL data will be stored
